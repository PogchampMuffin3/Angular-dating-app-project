import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-feed',
  imports: [RouterLink],
  templateUrl: './feed.html',
  styles: ``,
})
export class Feed {

}
